@require '../styl/common'
@require '../styl/page'

.main[data-page='works.html']
	display flex
	flex-direction column

	section.wip
		flex 1
		padding 4rem 5vw
		font-weight bold
		h1
			font-heading1()
			font-size 10vh
		p
			font-body()
			font-size 5vh
			font-weight bold
