{
  "dependencies": {
    "chokidar-cli": "^3.0.0",
    "compose-html": "file:../compose-html",
    "glob": "^8.1.0",
    "marked": "^4.2.12",
    "rimraf": "^3.0.2",
    "sharp": "^0.31.3"
  },
  "scripts": {
    "build": "compose-html --config compose-html.json",
    "dev": "python3 -m http.server --directory docs & chokidar 'src/**' -c 'yarn build'",
    "clean": "rimraf docs"
  }
}
