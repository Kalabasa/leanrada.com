{
  "dependencies": {
    "compose-html": "file:../compose-html"
  },
  "scripts": {
    "build": "compose-html -i src --root src/site -o docs",
    "dev": "serve docs & chokidar 'src/**' -c 'yarn build'",
    "clean": "rimraf docs"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "rimraf": "^3.0.2",
    "serve": "^14.1.2"
  }
}
